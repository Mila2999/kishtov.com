import * as React from 'react';
import Container from '@mui/material/Container';
import Store from './store';

export default function About() {
  return (
    <main className="flex bg-blue-100 min-h-screen flex-col">
      <Store />
    </main>
  );
}
